services:
  statsrag:
    image: ghcr.io/tony-myers/statsrag:0.3.17
    ports:
      - "8501:8501"
    volumes:
      - ./data:/data
    environment:
      - VO2RAG_DATA_DIR=/data
    restart: unless-stopped
